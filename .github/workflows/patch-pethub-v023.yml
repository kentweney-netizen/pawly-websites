name: patch-pethub-v023
on:
  workflow_dispatch:
  push:
    paths:
      - dapp/patch-pethub-v023-swap-till.py
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 dapp/patch-pethub-v023-swap-till.py
      - run: |
          git config user.name kentweney-netizen
          git config user.email kentweney@gmail.com
          git add dapp/src/petHub.tsx
          git diff --cached --quiet && exit 0
          git commit -m "v0.2.3 in-hub USDC/USDT/SOL swap to PAWLY till"
          git push origin HEAD:main
