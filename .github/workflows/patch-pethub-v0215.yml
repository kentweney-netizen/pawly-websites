name: patch-pethub-v0215
on:
  workflow_dispatch:
  push:
    paths:
      - dapp/patch-pethub-v0215-sponsor-only.py
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 dapp/patch-pethub-v0215-sponsor-only.py
      - run: |
          git config user.name kentweney-netizen
          git config user.email kentweney@gmail.com
          git add dapp/src/petHub.tsx
          git diff --cached --quiet && exit 0
          git commit -m "v0.2.15 Hub both hops sponsor-only no user SOL"
          git push origin HEAD:main
