name: patch-pethub-v0218
on:
  workflow_dispatch:
  push:
    paths:
      - dapp/patch-pethub-v0218-pet-reload.py
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 dapp/patch-pethub-v0218-pet-reload.py
      - run: |
          git config user.name kentweney-netizen
          git config user.email kentweney@gmail.com
          git add dapp/src/petHub.tsx
          git diff --cached --quiet && exit 0
          git commit -m "v0.2.18 reload pets on wallet change + recover adopts from chain"
          git push origin HEAD:main
