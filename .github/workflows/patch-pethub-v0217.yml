name: patch-pethub-v0217
on:
  workflow_dispatch:
  push:
    paths:
      - dapp/patch-pethub-v0217-await-b64.py
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 dapp/patch-pethub-v0217-await-b64.py
      - run: |
          git config user.name kentweney-netizen
          git config user.email kentweney@gmail.com
          git add dapp/src/petHub.tsx
          git diff --cached --quiet && exit 0
          git commit -m "v0.2.17 postSponsor send real base64 not Promise"
          git push origin HEAD:main
