name: patch-pethub-feed-cap
on:
  workflow_dispatch:
  push:
    paths:
      - dapp/patch-pethub-feed-cap.py
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 dapp/patch-pethub-feed-cap.py
      - run: |
          git config user.name kentweney-netizen
          git config user.email kentweney@gmail.com
          git add dapp/src/petHub.tsx
          git diff --cached --quiet && exit 0
          git commit -m "v0.10.3 hard cap 3 feeds per pet per day"
          git push origin HEAD:main
