name: patch-pethub-v0221
on:
  workflow_dispatch:
  push:
    paths:
      - dapp/patch-pethub-v0221-pay-all.py
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 dapp/patch-pethub-v0221-pay-all.py
      - run: |
          git config user.name kentweney-netizen
          git config user.email kentweney@gmail.com
          git add dapp/src/petHub.tsx
          git diff --cached --quiet && exit 0
          git commit -m "v0.2.21 Pet Hub sign-only swap for Jupiter and local key"
          git push origin HEAD:main
