name: patch-pethub-092
on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/patch-pethub-092.yml"
      - "dapp/patch-pethub-092.py"
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: python3 dapp/patch-pethub-092.py
      - run: |
          git config user.name "kentweney-netizen"
          git config user.email "kentweney@gmail.com"
          git add dapp/src/petHub.tsx
          if git diff --cached --quiet; then echo unchanged; exit 0; fi
          git commit -m "v0.9.2 Pet Hub: Netlify cert mail + scene BGM"
          git push origin HEAD:main
